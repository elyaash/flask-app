[tool.poetry]
name = "flask-app"
version = "0.1.0"
description = ""
authors = ["prajput <prajput@pch.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.2"
gunicorn = "^21.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
